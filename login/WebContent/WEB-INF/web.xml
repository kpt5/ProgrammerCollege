<?xml version="1.0" encoding="UTF-8"?>
<!-- web.xmlでは、画面表示に関する設定やStrutsフレームワークの有効化設定が行えます。 -->

<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xmlns="http://xmlns.jcp.org/xml/ns/javaee"
  xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
   http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd" id="WebApp_ID" version="3.1">

  <!-- display-name ＝ プロジェクト名 -->
  <!-- ~\.metadata\.plugins\org.eclipse.wst.server.core\tmp0\wtpwebapps にあるフォルダ名のことを指す -->
  <display-name>login</display-name>

  <welcome-file-list>
    <!-- <welcome-file>タグにプログラムしたファイルが、上から順に検索され、 -->
    <!-- 該当ファイルが見つかったら画面に表示される。 -->
    <!-- 「初期画面」（実行後最初に表示される画面）を指定 -->
    <welcome-file>login.jsp</welcome-file>
  </welcome-file-list>

<!--
<filter>タグと<filter-mapping>タグ、両方にある<filter-name>タグに同じ名前(struts2)を指定することで、
<filter-class>の設定と<url-pattern>の設定が結びつきます。
<filter>～<filter-mapping>は「このプロジェクト全体でstruts2を使います」という意味になる
 -->
  <filter>
    <filter-name>struts2</filter-name>
    <!-- Struts2の制御担当クラス（毎回固定） -->
    <filter-class>org.apache.struts2.dispatcher.ng.filter.StrutsPrepareAndExecuteFilter</filter-class>
  </filter>

  <filter-mapping>
    <filter-name>struts2</filter-name>
    <!-- 「適用する範囲（URL)」（/＊で固定）「このプロジェクト配下（／）のすべてのページ（＊）」を表す -->
    <url-pattern>/*</url-pattern>
  </filter-mapping>
</web-app>
